server:
  port: 70
spring:
  application:
    name: consumer70
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
#        namespace: spring-cloud-alibaba
#        group: TEST_GROUP